{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx lerna run dev --scope=@totallywired/react",
    "build": "npx lerna run build",
    "storybook": "npx lerna run storybook --scope=@totallywired/storybook"
  },
  "devDependencies": {
    "lerna": "^7.0.2"
  }
}
