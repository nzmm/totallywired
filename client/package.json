{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx lerna run dev --parallel",
    "build": "npx lerna run build --scope=@totallywired/react",
    "lint": "npx lerna run lint --scope=@totallywired/react",
    "prettier": "npx lerna run prettier"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "lerna": "^8.0.2",
    "prettier": "^3.0.3"
  }
}
