import React from "react";
import { TooltipProps } from "./Tooltip.library";
declare const Tooltip: ({ id, text, children, as }: TooltipProps) => React.JSX.Element;
export { Tooltip };
export type { TooltipProps };
